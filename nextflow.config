// settings to use local MinIO server for s3:// URL's
aws {
  // access key ve secret key django'da var onlar kullanabilir.
  accessKey = 'minio'
  secretKey = 'lnuqAW5IgfCof'
  client {
     endpoint = "http://172.18.0.4:9000"
  }
}
profiles {
  docker {
    docker.enabled = true
  }
}
